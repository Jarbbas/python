#tuples são estrututras de dados que não podem ser alterados,
#podem ser acedidos, mas não podemos alterar o seu conteudo
#semelhante a listas, mas só usamos tuples quando queremos
#valores que não serão alterados, tal como as constantes

coordinates = (4, 5)
print(coordinates[1])